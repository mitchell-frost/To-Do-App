import React from 'react';
import ReactDOM from 'react-dom';
import './App.css';

function Header() {
    return (
        <header className="navbar">
        <p>To-Do App</p>
      </header>
    );
}

export default Header;